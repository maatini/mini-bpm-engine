---
description: Verify the Tauri UI (TypeScript build check)
---
// turbo-all
1. cd desktop-tauri && npm run build

If the build fails:
- Read the full TypeScript error output
- Fix the issue in the relevant `.tsx` or `.ts` file
- Re-run `npm run build` before continuing
